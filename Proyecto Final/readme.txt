Para crear el ejecutable, situarse en la carpeta src con los 3 archivos de código, abrir una terminal
y ejecutar el siguiente comando:

ghc --make FullHousePuzzle.hs

El archivo ejecutable se llamará FullHousePuzzle y aparecerá en la carpeta.